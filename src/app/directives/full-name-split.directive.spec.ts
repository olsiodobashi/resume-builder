import { FullNameSplitDirective } from './full-name-split.directive';

describe('FullNameSplitDirective', () => {
  it('should create an instance', () => {
    const directive = new FullNameSplitDirective();
    expect(directive).toBeTruthy();
  });
});
